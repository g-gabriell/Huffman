#include <stdio.h>
#include <stdlib.h>

#include "heap.h"

/*
struct arvore{
    sub_arvore* raiz;
};

struct sub_arvore{
    char*   grupo;
    int     tam_grupo;
    int     freq;

    sub_arvore_t* f_esq;
    sub_arvore_t* f_dir;
};

arvore_t* cria_arvore()
{
    arvore_t *p = NULL;

	p = (arvore_t*) malloc(sizeof(arvore_t));

	if (p == NULL)	{
		perror("cria_arvore:");
		exit(EXIT_FAILURE);
	}

	p->raiz = NULL;

	return p;
}
*/
